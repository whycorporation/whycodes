# test(tui): whycodes-tui to 100% (47/63)

Parent: #57.

## Problem
~86.3% lines, 47/63 files. `app.rs`, `run/*`, `md_stream.rs`, `input.rs`, `clipboard.rs`, `cell_grid.rs` are the usual miss list. No floor.

## Surfaces
- `crates/tui/src/app.rs`, `run/*`, `md_stream.rs`, `input.rs`, `clipboard.rs`, `cell_grid.rs`

## Proposal
Headless/backend tests (ratatui TestBackend). At least one snapshot/golden for a rendered frame. Do not require a real tty. Respect `docs/knowhow.md` (idle 0 draws/s, no extra redraw).

## Acceptance
- [ ] `FLOORS` includes `("whycodes-tui", 100.0)`.
- [ ] File-test ratio >= 80%.
- [ ] One snapshot/golden render test.
- [ ] `cargo test -p whycodes-tui` green; `cargo build -p whycodes-cli` smoke; coverage OK 100%.

## Validation
```bash
cargo test -p whycodes-tui
cargo build -p whycodes-cli
scripts/coverage.sh
```
