# test(llm): whycodes-llm to 100% (27/37)

Parent: #57. Prefer after `agent` so provider mocks can be reused.

## Problem
~83.6% lines. Provider HTTP paths and fallback/catalog branches are the bulk of the miss. No floor.

## Surfaces
- `crates/llm/src/provider.rs`
- `crates/llm/src/providers/*` (anthropic, antigravity, codeassist, codex, copilot, google, groq, deepseek, custom, …)
- `crates/llm/src/{fallback,model_catalog}.rs`

## Proposal
`httpmock` / wiremock-style fixtures already used in the crate — extend them. No live network.

## Acceptance
- [ ] `FLOORS` includes `("whycodes-llm", 100.0)`.
- [ ] File-test ratio >= 80%.
- [ ] `cargo test -p whycodes-llm` green; coverage OK 100%.

## Validation
```bash
cargo test -p whycodes-llm
scripts/coverage.sh
```
