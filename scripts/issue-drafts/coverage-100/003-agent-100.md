# test(agent): whycodes-agent to 100% (20/28)

Parent: #57. Suggested after Phase 1; before `llm` if sharing mocks.

## Problem
`whycodes-agent` is ~65.4% lines — lowest among large crates besides `cli`/`lsp`. No per-crate floor. Turn loop, gates, compaction, and dispatch are the product core.

## Surfaces
- `crates/agent/src/agent/{compact,dispatch,gate,turn,mod}.rs`
- `crates/agent/src/{context_files,tool_stream,speculative_read,routing,swarm,magic_keywords,events}.rs`

## Proposal
Unit-test gate deny/allow, compact triggers, dispatch serialization vs concurrent readonly, routing, speculative read. Use scripted LLM / fake tools; no live provider.

## Acceptance
- [ ] `FLOORS` includes `("whycodes-agent", 100.0)`.
- [ ] File-test ratio >= 80%.
- [ ] `cargo test -p whycodes-agent` green; `scripts/coverage.sh` OK 100%.

## Validation
```bash
cargo test -p whycodes-agent
scripts/coverage.sh
```
