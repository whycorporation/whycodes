# test(tools): whycodes-tools to 100% (32/56)

Parent: #57. Largest crate — one PR, split commits by tool group.

## Problem
~86.0% lines, 32/56 files with tests. Executor, file tools, shell, grep, and `agent_tools/*` have uncovered error paths. No floor.

## Surfaces
- `crates/tools/src/agent_tools/*`
- `crates/tools/src/{executor,shell,grep}.rs`
- `crates/tools/src/file/*`
- web/github already have `mod tests` — fill remaining branches.

## Proposal
Tempdir fixtures for file/shell; mock HTTP for web/github. Split commits: file tools, shell/grep, agent_tools, executor, remainder.

## Acceptance
- [ ] `FLOORS` includes `("whycodes-tools", 100.0)`.
- [ ] File-test ratio >= 80%.
- [ ] `cargo test -p whycodes-tools` green; coverage OK 100%.

## Validation
```bash
cargo test -p whycodes-tools
scripts/coverage.sh
```
