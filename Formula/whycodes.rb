# typed: false
# frozen_string_literal: true

# Generated by scripts/update_homebrew_formula.sh for v0.6.4.
# release.yml re-runs that script after each tagged release.
# Do not hand-edit sha256 values.
#
# Install (Homebrew 6+ requires tap trust):
#   brew tap whycorporation/whycodes https://github.com/whycorporation/whycodes
#   brew install whycorporation/whycodes/whycodes
#
# If already tapped:
#   brew trust --formula whycorporation/whycodes/whycodes
#   brew install whycodes
#
# Source tip (needs a Rust toolchain):
#   brew install --HEAD whycorporation/whycodes/whycodes

class Whycodes < Formula
  desc "Terminal coding agent written in Rust"
  homepage "https://why.codes"
  version "0.6.4"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/whycorporation/whycodes/releases/download/v0.6.4/whycodes-aarch64-apple-darwin.tar.gz"
      sha256 "cdfe92fbe8ac2f4f79118dc7b63bc1534a4f9105d37e08290142ed1f4396c084"
    end

    on_intel do
      url "https://github.com/whycorporation/whycodes/releases/download/v0.6.4/whycodes-x86_64-apple-darwin.tar.gz"
      sha256 "54336047046f364cacf04b5981bc2c435b286fcb1ffc24b706973fea15d438b8"
    end
  end

  on_linux do
    on_intel do
      url "https://github.com/whycorporation/whycodes/releases/download/v0.6.4/whycodes-x86_64-unknown-linux-gnu.tar.gz"
      sha256 "9aff5329bb661dc954a9faea777d68565491328977d9df66ff4e196edf10b55f"
    end
  end

  head do
    url "https://github.com/whycorporation/whycodes.git", branch: "main"
    depends_on "rust" => :build
  end

  livecheck do
    url "https://github.com/whycorporation/whycodes/releases/latest"
    strategy :github_latest
  end

  def install
    if build.head?
      system "cargo", "install", "--locked", "--features", "bundled-sqlite", *std_cargo_args(path: "crates/cli")
    elsif File.exist?("whycodes")
      bin.install "whycodes"
    elsif File.exist?("whycode")
      # v0.1.0 archives still ship the pre-rebrand binary name.
      bin.install "whycode" => "whycodes"
    else
      odie "archive did not contain a whycodes binary"
    end
    generate_completions_from_executable(bin/"whycodes", "completions")
  end

  def caveats
    <<~EOS
      The binary is unsigned, so macOS Gatekeeper may warn on first run.
      Update with `brew upgrade whycodes` rather than `whycodes upgrade`.
    EOS
  end

  test do
    assert_match "whycodes", shell_output("#{bin}/whycodes --version")
    assert_match "whycodes", shell_output("#{bin}/whycodes completions bash")
  end
end
